{
  "name": "@raypx/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "git clean -fdX -e node_modules -e .turbo",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "with-env": "dotenvx run -f ../../.env --"
  },
  "dependencies": {
    "@raypx/env": "workspace:*",
    "redis": "^5.10.0"
  },
  "devDependencies": {
    "@raypx/config": "workspace:*",
    "@vitest/ui": "catalog:",
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
