import { createEnv, z } from "@raypx/config"

export const envs = () =>
  createEnv({
    client: {
    },
    shared: {
      NODE_ENV: z.enum(["development", "production"]).default("development"),
    },
    server: {
    },
  })
